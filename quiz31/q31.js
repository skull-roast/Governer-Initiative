"use strict";
// import { web } from "./q30.js";
const web = ["Faraz", "Nadeem", "Admin", "Ramla Eman", "Affan"];
web.splice(0, web.length);
// Check if the list is empty
if (web.length === 0) {
    console.log("We need to find some users!");
}
